/**
 * 
 */
function find(array, value) {
	console.log(array.indexOf(value));
}
 let styles =[ 'Jazz', 'Blues', 'Rep', 'Reggae', 'Classics', 'Rock and roll' ];
find(styles, "Jazz");
find(styles, "jazz");
